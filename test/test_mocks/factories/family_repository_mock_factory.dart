// EduLift Mobile - Family Repository Mock Factory
// Phase 2.3: Separate factory per repository as required by execution plan


/// Family Repository Mock Factory
/// TRUTH: Provides consistent family-related mock behavior
class FamilyRepositoryMockFactory {
  // FamilyRepositoryCore has been removed and merged into FamilyRepositoryImpl
  // Use MockFamilyRepository directly instead

  // Note: Children operations are now part of FamilyRepository
  // Use createFamilyRepository() and configure children operations through that

  // ========================================
  // HELPER METHODS
  // ========================================

  // Helper methods moved to individual test files where needed
}
